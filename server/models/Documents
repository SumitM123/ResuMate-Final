const mongoose = require('mongoose');

/*
const Documents = mongoose.Schema( {
    googleID: {
        type: String,
        require: true,
        unique, true
    },
    allDocuments: 
}) 
*/
module.exports = Document;